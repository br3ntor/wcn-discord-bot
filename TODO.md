[ ] Make configurable any parts of the code I change between servers.

[ ] Determine if switching to issuing commands with RCON will mean less permissions I have to grant on the system.
    maybe even restarts could be just issuing the quit command and letting systemd auto restart the server. That might eliminate
    all the special permissions.

[ ] New command to display current donation goal progress.
